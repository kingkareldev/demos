/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.name, this.routeName);

  final int id;
  final String name;
  final String routeName;
}
